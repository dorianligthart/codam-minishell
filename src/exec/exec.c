#include "minishell.h"
#include <stdio.h>

bool	ms_exec(t_shell *sh)
{
	//fprintf(stderr, "\nms_exec_go() not implemented!\n");
	(void)sh;
	return (true);
}

bool	ms_initexec(t_shell *sh)
{
	//fprintf(stderr, "\nms_exec_init() not implemented!\n");
	(void)sh;
	return (true);
}

